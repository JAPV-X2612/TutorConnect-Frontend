import TutorDashboard from '@/src/screens/TutorDashboard';

/**
 * Tutor dashboard tab.
 *
 * Thin route file — actual implementation lives in `src/screens/TutorDashboard`
 * so the screen logic stays out of the Expo Router tree.
 *
 * @author TutorConnect Team
 */
export default TutorDashboard;
